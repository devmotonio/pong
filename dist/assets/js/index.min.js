const GAME_WIDTH=700,GAME_HEIGHT=500;let canvas=document.getElementById("gameScreen"),ctx=canvas.getContext("2d"),game=new Game(GAME_WIDTH,GAME_HEIGHT);function gameLoop(){ctx.clearRect(0,0,GAME_WIDTH,GAME_HEIGHT),game.update(ctx),requestAnimationFrame(gameLoop)}game.start(),requestAnimationFrame(gameLoop);